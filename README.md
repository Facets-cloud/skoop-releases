# skoop-releases
Public releases of the skoop CLI. Assets only; the manifest is served by skoopd.

Install with `curl -fsSL https://skoop.facetsapp.cloud/install.sh | sh`. A laptop that already has skoop upgrades on its own.
